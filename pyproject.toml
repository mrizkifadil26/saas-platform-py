[tool.ruff]
line-length = 100
target-version = "py311"
src = ["services", "packages"]

[tool.pytest.ini_options]
testpaths = [
    "packages/iam/tests",
]

[tool.uv.workspace]
members = [
    "packages/iam",
    "packages/shared/db",
]
