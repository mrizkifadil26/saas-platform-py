[project]
name = "taskqueue-beanstalk"
version = "0.1.0"
description = ""
authors = [
    {name = "Muhammad Rizki Fadillah",email = "mrizkifadil26@gmail.com"}
]
requires-python = ">=3.11"
dependencies = [
]

[tool.poetry]
name = "taskqueue-beanstalk"
version = "0.1.0"
packages = [{ include = "taskqueue_beanstalk", from = "src" }]

[tool.poetry.dependencies]
python = "^3.11"
greenstalk = "^2.0.0"
taskqueue = { path = "../taskqueue", develop = true }

[tool.poetry.group.dev.dependencies]
pytest = "^8.0"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
